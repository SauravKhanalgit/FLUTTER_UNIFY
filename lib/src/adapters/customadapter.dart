import 'package:flutter_unify/flutter_unify.dart';

import 'auth_adapter.dart';

abstract class CustomAdapter extends AuthAdapter {
  // Custom adapter implementation - made abstract to avoid implementing all methods
}
